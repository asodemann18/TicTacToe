class Player {
  constructor(token, id) {
    this.id = id || Date.now();
    this.token = token;
    this.wins = [];
  }

  // saveWinsToStorage() {
  //
  // }
  //
  // retrieveWinsFromStorage() {
  //
  // }
}
